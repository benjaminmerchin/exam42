vim ~/.myvimrc

set number
set mouse=a
syntax on
set ruler

Librairies pour GNL :
stdlib (malloc)
unistd (read)
stdio (pour printf dans mon main de test)

pas besoin des autres librairies open et close vu qu'on travaille uniquement avec l'entree standard



Librairies pour ft_printf
unistd (write)
stdarg (va_start & familly)
stdio (printf pour tester dans le main de test)

Pense bete :

data.str = (char *)str; // cast la chaine dans la struct
va_start(data.arg, str); // data.arg de type va_list
va_arg(data->arg, type); // pour lire le type
va_end(str); // a la fin